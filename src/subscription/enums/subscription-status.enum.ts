export enum SubscriptionStatusEnum {
  CREATED = "created",
  PAID = "paid",
  NOT_PAID = "not_paid",
  IN_PROGRESS = "in_progress",
  PROCESSED = "processed",
  EXPIRED = "expired",
  FAILED = "failed",
}
